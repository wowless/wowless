name: modules
type:
  mapof:
    key: string
    value:
      taggedunion:
        deps:
          setof:
            ref:
              schema: modules
        root:
          record:
            default:
              type: string
