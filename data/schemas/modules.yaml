name: modules
type:
  mapof:
    key: string
    value:
      taggedunion:
        deps:
          setof:
            ref:
              schema: module
        root:
          record:
            default:
              type: string
