name: schematype
type:
  taggedunion:
    any: tag
    boolean: tag
    literal: string
    mapof:
      record:
        key:
          required: true
          type:
            schema: schematype
        value:
          required: true
          type:
            schema: schematype
    number: tag
    record:
      mapof:
        key: string
        value:
          record:
            required:
              type: boolean
            type:
              required: true
              type:
                schema: schematype
    ref:
      record:
        negative:
          type: boolean
        schema:
          required: true
          type:
            taggedunion:
              api: tag
              cvar: tag
              enum: tag
              event: tag
              family: tag
              gametype: tag
              impl: tag
              luaobject: tag
              module: tag
              schema: tag
              scripttype: tag
              sql: tag
              stringenum: tag
              structure: tag
              uiobject: tag
              uiobjectimpl: tag
              xml: tag
    schema:
      ref:
        schema: schema
    sequenceof:
      schema: schematype
    setof:
      schema: schematype
    string: tag
    table: tag
    tag: tag
    taggedunion:
      mapof:
        key: string
        value:
          schema: schematype
