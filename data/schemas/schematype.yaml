---
name: schematype
type:
  oneof:
  - literal: any
  - literal: boolean
  - literal: schematype
  - literal: string
  - record:
      enumset:
        required: true
        type:
          record:
            nonempty:
              type: boolean
            values:
              required: true
              type:
                sequenceof: string
  - record:
      literal:
        required: true
        type: string
  - record:
      mapof:
        required: true
        type: schematype
  - record:
      oneof:
        required: true
        type:
          sequenceof: schematype
  - record:
      record:
        required: true
        type:
          mapof:
            record:
              required:
                type: boolean
              type:
                required: true
                type: schematype
  - record:
      ref:
        required: true
        type:
          oneof:
          - literal: state
          - literal: structure
  - record:
      sequenceof:
        required: true
        type: schematype
