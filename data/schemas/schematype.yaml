name: schematype
type:
  taggedunion:
    any: tag
    boolean: tag
    flag: tag
    hierarchy:
      record:
        fields:
          required: true
          type:
            schema: record
        parent:
          required: true
          type: string
        unique:
          required: true
          type:
            setof: string
    literal: string
    mapof:
      record:
        key:
          required: true
          type:
            schema: schematype
        value:
          required: true
          type:
            schema: schematype
    number: tag
    record:
      schema: record
    ref:
      record:
        negative:
          type: flag
        schema:
          required: true
          type:
            ref:
              schema: domains
    schema:
      ref:
        schema: schemas
    sequenceof:
      schema: schematype
    setof:
      schema: schematype
    string: tag
    table: tag
    tag: tag
    taggedunion:
      mapof:
        key: string
        value:
          schema: schematype
