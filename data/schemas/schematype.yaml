---
name: schematype
type:
  oneof:
  - any
  - boolean
  - string
  - record:
      record:
        required: true
        type:
          mapof: schematype
  - record:
      enum:
        required: true
        type:
          sequenceof: string
  - record:
      enumset:
        required: true
        type:
          record:
            nonempty:
              type: boolean
            values:
              required: true
              type:
                sequenceof: string
  - record:
      mapof:
        required: true
        type: schematype
  - record:
      oneof:
        required: true
        type:
          sequenceof: schematype
  - record:
      sequenceof:
        required: true
        type: schematype
