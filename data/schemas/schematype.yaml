---
name: schematype
type:
  taggedunion:
    boolean: tag
    literal: string
    mapof:
      record:
        key:
          required: true
          type:
            schema: schematype
        value:
          required: true
          type:
            schema: schematype
    number: tag
    oneof:
      sequenceof:
        schema: schematype
    record:
      mapof:
        key: string
        value:
          record:
            required:
              type: boolean
            type:
              required: true
              type:
                schema: schematype
    ref:
      taggedunion:
        api: tag
        cvar: tag
        enum: tag
        event: tag
        family: tag
        gametype: tag
        impl: tag
        schema: tag
        sql: tag
        stringenum: tag
        structure: tag
        uiobject: tag
        uiobjectimpl: tag
        xml: tag
    schema:
      ref: schema
    sequenceof:
      schema: schematype
    string: tag
    table: tag
    tag: tag
    taggedunion:
      mapof:
        key: string
        value:
          schema: schematype
