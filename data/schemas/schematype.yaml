---
name: schematype
type:
  oneof:
  - enum:
    - any
    - boolean
    - schematype
    - string
  - record:
      record:
        required: true
        type:
          mapof:
            record:
              required:
                type: boolean
              type:
                required: true
                type: schematype
  - record:
      enum:
        required: true
        type:
          sequenceof: string
  - record:
      enumset:
        required: true
        type:
          record:
            nonempty:
              type: boolean
            values:
              required: true
              type:
                sequenceof: string
  - record:
      mapof:
        required: true
        type: schematype
  - record:
      oneof:
        required: true
        type:
          sequenceof: schematype
  - record:
      ref:
        required: true
        type:
          enum:
          - structure
  - record:
      sequenceof:
        required: true
        type: schematype
