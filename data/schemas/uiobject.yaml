---
name: uiobject
type:
  record:
    fields:
      mapof:
        record:
          type: string
    inherits:
      sequenceof: string
    methods:
      mapof:
        record:
          fields:
            sequenceof: string
          status: string
