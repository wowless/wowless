name: enums
type:
  mapof:
    key: string
    value:
      record:
        values:
          required: true
          type:
            mapof:
              key: string
              value: enumvalue
