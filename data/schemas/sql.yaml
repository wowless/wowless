---
name: sql
type:
  mapof:
    key: string
    value:
      record:
        indexes:
          type:
            mapof:
              key: string
              value: string
        table:
          type: boolean
        type:
          required: true
          type:
            oneof:
            - literal: cursor
            - literal: lookup
