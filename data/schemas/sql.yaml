name: sql
type:
  mapof:
    key: string
    value:
      record:
        indexes:
          type:
            mapof:
              key: string
              value: string
        table:
          type: flag
        type:
          required: true
          type:
            taggedunion:
              cursor: tag
              lookup: tag
