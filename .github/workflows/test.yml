name: test
on:
  pull_request:
  push:
    branches:
    - main
env:
  VCPKG_DEFAULT_BINARY_CACHE: ${{ github.workspace }}/vcpkg-cache
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v6
      with:
        submodules: true
    - run: mkdir vcpkg-cache
    - uses: actions/cache@v5
      with:
        path: vcpkg-cache
        key: vcpkg-devcontainer-${{ github.run_id }}
        restore-keys: vcpkg-devcontainer
    - uses: docker/login-action@v4
      with:
        registry: ghcr.io
        username: ${{ github.repository_owner }}
        password: ${{ secrets.GITHUB_TOKEN }}
    - uses: devcontainers/ci@v0.3
      with:
        env: |
          GITHUB_TOKEN=${{ secrets.GITHUB_TOKEN }}
          VCPKG_DEFAULT_BINARY_CACHE=/workspaces/wowless/vcpkg-cache
        imageName: ghcr.io/wowless/wowless-devcontainer
        refFilterForPush: refs/heads/main
        runCmd: pre-commit run -a -v
    - uses: actions/upload-artifact@v7
      if: success() || failure()
      with:
        name: test-results
        path: |
          out/
          test.out
  build:
    strategy:
      fail-fast: false
      matrix:
        config:
        - name: Linux
          os: ubuntu-latest
        - name: MacOS
          os: macos-latest
        - name: Windows
          os: windows-latest
    runs-on: ${{ matrix.config.os }}
    steps:
    - if: ${{ matrix.config.os == 'ubuntu-latest' }}
      run: sudo apt-get install autoconf-archive libltdl-dev
    - uses: actions/checkout@v6
      with:
        submodules: true
    - run: mkdir ${{ env.VCPKG_DEFAULT_BINARY_CACHE }}
    - uses: actions/cache@v5
      with:
        path: ${{ env.VCPKG_DEFAULT_BINARY_CACHE }}
        key: vcpkg-${{ runner.os }}-${{ github.run_id }}
        restore-keys: vcpkg-${{ runner.os }}
    - uses: lukka/get-cmake@v4.3.0
    - run: cmake --preset default
    - run: cmake --build --preset default --verbose
    - run: cmake --build build --config Debug --verbose
