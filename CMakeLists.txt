cmake_minimum_required(VERSION 3.24)
project(wowless)
add_compile_options(-D_GNU_SOURCE -DNDEBUG -flto -O3)
add_subdirectory(vendor/elune)
add_executable(wowless wowless.c wowless/ext.c)
target_include_directories(wowless PRIVATE $<TARGET_PROPERTY:liblua,INTERFACE_INCLUDE_DIRECTORIES>)
target_link_libraries(wowless liblua)
