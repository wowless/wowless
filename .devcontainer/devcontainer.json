{
    "build": {
        "context": "..",
        "dockerfile": "Dockerfile"
    },
    "features": {
        "docker-from-docker": {},
        "github-cli": {}
    },
    "initializeCommand": "git submodule update --init --recursive",
    "name": "lua-wow-tools/wowless",
    "postCreateCommand": "pre-commit install && pre-commit run -a -v --show-diff-on-failure",
    "remoteUser": "vscode"
}
